# Cubby

Personal camera/album Flutter app.

See [docs/design.md](docs/design.md) for the design document.

## Getting Started

```bash
flutter pub get
flutter run
```

## Notes

This scaffold is derived from a clean Flutter app template. After cloning, run
`flutter doctor` and `flutter pub upgrade --major-versions` to align with the
latest Flutter SDK on your machine.
